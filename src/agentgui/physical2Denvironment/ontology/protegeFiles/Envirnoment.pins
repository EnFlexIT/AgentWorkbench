; Thu Nov 04 22:38:57 CET 2010
; 
;+ (version "3.3.1")
;+ (build "Build 430")

([ActiveObject] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE))

([Movement] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE)
	(%3AJADE-JAVA-CODE "/**\n    * This method calculates movement's speed\n    * This method calculates the speed from the movement's x and y components using the Pythagorean theorem\n    * @return The speed\n    */\n   public float getSpeed(){\n	   return (float) Math.sqrt(xPosChange*xPosChange + yPosChange*yPosChange);\n   }"))

([PassiveObject] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE))

([Physical2DEnvironment] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE))

([Physical2DObject] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE)
	(%3AJADE-JAVA-CODE "/**\n    * Checks if this Physical2DObject completely contains another one \n    * @param object The other Physical2DObject\n    * @return True or false\n    */\n   public boolean objectContains(Physical2DObject object){\n	   java.awt.geom.Rectangle2D.Float myRect = new java.awt.geom.Rectangle2D.Float(getPosition().getXPos(), getPosition().getYPos(), getSize().getWidth(), getSize().getHeight());\n	   java.awt.geom.Rectangle2D.Float objectRect = new java.awt.geom.Rectangle2D.Float(object.getPosition().getXPos(), object.getPosition().getYPos(), object.getSize().getWidth(), object.getSize().getHeight());\n	   \n	   return myRect.contains(objectRect);\n   }\n   \n   /**\n    * Checks if this Physical2DObject intersects another one\n    * @param object The other Physical2DObject\n    * @return True or false\n    */\n   public boolean objectIntersects(Physical2DObject object){\n	   java.awt.geom.Rectangle2D.Float myRect = new java.awt.geom.Rectangle2D.Float(getPosition().getXPos(), getPosition().getYPos(), getSize().getWidth(), getSize().getHeight());\n	   java.awt.geom.Rectangle2D.Float objectRect = new java.awt.geom.Rectangle2D.Float(object.getPosition().getXPos(), object.getPosition().getYPos(), object.getSize().getWidth(), object.getSize().getHeight());\n	   \n	   return myRect.intersects(objectRect);\n   }"))

([PlaygroundObject] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE))

([Position] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE))

([project+annotation] of  %3APROJECT-ANNOTATION

	(%3ALOCATION "D:\\Anwendungsdaten\\Eclipse Workspace\\AgentGUI\\src")
	(%3AONTOLOGYNAME "Environment")
	(%3APACKAGE "agentgui.physical2Denvironment.ontology")
	(%3ASUPPORT "j2se"))

([Scale] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE))

([Size] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE))

([StaticObject] of  %3AJADE-CLASS

	(%3AJADE-IGNORE FALSE))
